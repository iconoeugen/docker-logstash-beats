filter {
  if [type] == "apache_error" {
    grok {
      match => { "message" => "%{HTTPD24_ERRORLOG}" }
    }
    date {
      match => [ "timestamp" , "EEE MMM dd HH:mm:ss.SSSSSS yyyy" ]
    }
  }
}
